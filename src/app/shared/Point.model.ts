export class Point {
  x: number;
  y: number;
  clr: string;
  size: number;

  constructor(x: number, y: number, clr: string, size: number) {
    this.x = x;
    this.y = y;
    this.clr = clr;
    this.size = size;
  }
}
